import React from 'react';

const Topbar = () => {
  return (
    <header style={styles.topbar}>
      <h1>f</h1>
    </header>
  );
};

const styles = {
  topbar: {
    backgroundColor: '#4CAF50',
    color: '#fff',
    padding: '1rem',
    textAlign: 'center',
  },
};

export default Topbar;
